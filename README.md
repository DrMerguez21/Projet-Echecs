pour push des trucs
git add <ce que vous voulez push>
git commit -m "infos que vous voulez rajouter"
git push

s'il y a un pb, git pull, et git status de temps en temps au cas ou

pour compiler faire make clean, make puis ./jeu
